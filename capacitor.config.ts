import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alvisiapro.ultimate',
  appName: 'Alvisia Pro Ultimate',
  webDir: 'dist'
};

export default config;
